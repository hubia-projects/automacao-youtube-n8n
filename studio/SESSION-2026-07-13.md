# Sessão 2026-07-12/13 — Rearquitetura Studio v2 (resumo para handoff)

## O que foi feito (TUDO verificado com serviços reais)
- **Fases 0-8 do roadmap completas numa sessão.** Greenfield Python 3.12 (uv)
  em `studio/`; 4.047 linhas substituem ~27.700 do legacy Node. 42/42 testes.
- Diagnóstico do legacy: pool faminto + matching texto-sobre-texto + sem
  revisor holístico. Decisões: DAG próprio (não LangGraph/Temporal), LanceDB,
  SigLIP-base + briefs SEMPRE em inglês, FFmpeg puro, revisor Gemini vídeo,
  fail-closed em tudo, prompts em ficheiros versionados.
- Run real completo `smoke-real-f3`: tema→script multi-pass→TTS multivozes→
  whisper local→cenas→briefs→matching→timeline→proxy→revisor→final.mp4 1080p
  114.0s (sync exato c/ narração 114.1s)→metadata+thumbnail. **Custo $0.234.**
- Revisor Gemini VIU o vídeo e deu 45/100 honesto ("blueberry tart instead of
  pastel de belém") — causa: biblioteca só tem ~30 shots. E2E sabotagem passa.

## Aprendizagens técnicas críticas (não redescobrir)
- torch **2.5.1+cu121 pinado** — última versão com Pascal/GTX 1050 Ti; VRAM
  partilhada com desktop → embedder tem fallback CPU automático.
- Gemini: `gemini-2.5-pro` dá 404 a novos users → usar aliases
  `gemini-pro-latest`/`gemini-flash-latest`. Vision JSON parte-se → retry
  temp 0 + skip por-shot. sentencepiece+protobuf necessários p/ SigLIP.
- FFmpeg: zoompan exige `s=WxH` (default 1280x720); concat `-c copy` gera PTS
  que partem xfade → re-encode; gaps de pausa da narração cobertos com
  target-por-entrada + tpad clone; narração é o master de sync.
- Gates Telegram NÃO-bloqueantes (`pending:<msg_id>` no state); params do run
  persistem em run.json; só o runner grava run.json (stages mutam ctx.state).
- Escada de relaxamento no matching: base→drop_people→quality_3→
  drop_must_have→reuse; **must_not nunca cede**.

## Estado operacional
- Operar: **`studio/OPERATIONS.md`** (comandos start/monitor + FAQ).
- Multivozes UP:5050. Biblioteca 30 shots (meta ≥2000 — `ingest seed`, 1
  noite, ~$1-2). `data/music/` vazio (mp3 manuais → ducking ativa só).
  Upload real nunca executado (classificador bloqueou; `--upload` pronto).
- Sem cron (decisão do utilizador). Fluxo completo será pedido via chat, COM
  upload. Cutover formal cancelado; legacy `pipeline/` arquivável.

## PRÓXIMO TRABALHO = backlog em OPERATIONS.md §BACKLOG
1. `studio watch`: daemon poll de waiting_approval → resume após clique.
2. Fallback ElevenLabs no TTS (chave no .env; portar do legacy).
3. Auto-arranque multivozes (healthcheck + docker compose up antes do s04).
4. `studio cleanup`: manter 10 runs recentes; NUNCA tocar em data/library/;
   compactar media de shots não usados (crf alto) preservando embeddings.
5. Menores: threshold cobertura discovery; beat grid c/ música; contact sheets.
